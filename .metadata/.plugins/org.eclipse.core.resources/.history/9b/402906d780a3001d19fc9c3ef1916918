package day5;

import day4.MemberTable;

// 메인은 테스트용, 입력(X) 출력(X), 특정 클래스를 실행하는 역활만 (메인은 지시만 하고 게으른 페이지어야 함.)

public class Main4 {
	public static void main(String[] args) {

		// 콘솔에서 입력한 앙목을 기반으로 MemberTale 타입으로 반환해주세요.
	MemberTable member = Input.inputData();
		System.out.println(member.toString());  // member에 값이 잘 들어갔는지 확인해본것임. 주용한거 아니니 지워도 됨.
	
		Output.printMember(Input.inputData());
	}

}
